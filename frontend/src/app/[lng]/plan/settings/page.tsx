'use client';

import React from 'react';

export default function SettingsPage({ params }: { params: Promise<{ lng: string }> }) {
  // Redirect logic now handled in SettingsLayout
  // This page only renders on mobile portrait to show the navigation list
  return null;
}
